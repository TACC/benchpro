local install_path = "<<<install_path>>>"

setenv("<<<caps_code>>>_DIR", install_path)
prepend_path("PATH",     pathJoin(install_path, "<<<bin_dir>>>"))

always_load(<<<mods>>>)
family("<<<caps_code>>>")
